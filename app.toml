# app.toml — 唯一事實來源（Single Source of Truth）
# 修改此處即可變更軟體名稱與版次，會連動：前端標題、關於頁、打包輸出檔名。
# 其他檔案一律從此取值，請勿在別處硬編碼名稱或版次。

app_name = "AhhOuch_Manager"
version  = "2.7.0"

# 伺服器預設綁定位址與埠（初期單機單人；日後開放區網改 host = "0.0.0.0"）
[server]
host = "127.0.0.1"
port = 8000
